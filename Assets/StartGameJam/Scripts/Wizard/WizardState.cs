namespace StartGameJam.Scripts.Wizard
{
    public enum WizardState
    {
        Idle = 0,
        Run = 10,
        Jump = 20,
        Fall = 30,
        Attack = 40,
        TakeDamage = 50,
        Death = 60
    }
}